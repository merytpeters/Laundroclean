



// Hook
/*export const useServices = () => {
  return useQuery({
    queryKey: ['services', 'promoCodes'],
    queryFn: async () => {
      const serviceResponse = await apiRequest<ServicesPromoResponse>('services');
      // const promo
    }
  })
} */